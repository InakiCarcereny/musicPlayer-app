import { createSlice } from '@reduxjs/toolkit'

const initialState = []

export const playlistsSlice = createSlice({
  name: 'playlists',
  initialState,
  reducers: {
  }
})

const {  } = playlistsSlice.actions
 
export default playlistsSlice.reducer